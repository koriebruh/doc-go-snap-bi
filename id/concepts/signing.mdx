---
title: "Signing"
description: "Signing request HMAC dan RSA, formula string-to-sign, dan parsing key"
---

Standar SNAP mendefinisikan dua mode signing, disepakati per partner saat
registrasi: **symmetric** (HMAC-SHA512) dan **asymmetric** (SHA256withRSA).
Package ini mengimplementasikan keduanya sebagai fungsi biasa —
`HeaderBuilder.Build()` dan `TokenManager` memanggilnya, tapi Anda bisa
memakainya langsung untuk testing atau untuk mengimplementasikan verifier
berbasis `KeyStore`.

## Symmetric (HMAC-SHA512)

```go
func SignSymmetric(clientSecret, stringToSign string) string
func VerifySymmetric(clientSecret, stringToSign, signature string) bool
```

`SignSymmetric` menghitung signature HMAC-SHA512 dari `stringToSign`
menggunakan `clientSecret` sebagai key, mengembalikan hex huruf kecil.
`VerifySymmetric` memeriksa signature dengan cara yang sama, memakai
perbandingan constant-time.

## Asymmetric (SHA256withRSA)

```go
func SignAsymmetric(signer crypto.Signer, stringToSign string) (string, error)
func VerifyAsymmetric(pub crypto.PublicKey, stringToSign, signature string) error
```

`SignAsymmetric` menandatangani dengan SHA256withRSA (PKCS#1 v1.5),
mengembalikan hex huruf kecil. Ia menerima `crypto.Signer`, bukan
`*rsa.PrivateKey` konkret, agar key berbasis HSM/KMS bisa dipasang tanpa
perubahan API. `VerifyAsymmetric` mengembalikan error non-nil jika `pub`
bukan RSA key, `signature` bukan hex valid, atau signature tidak
terverifikasi.

Kedua fungsi menolak RSA key lebih kecil dari **2048 bit** — batas minimum
modulus 256-byte milik standar sendiri — karena key yang lebih lemah secara
praktis bisa dipalsukan, yang paling penting di sisi verify di mana public
key bisa berasal dari sertifikat yang didaftarkan partner.

```go
func ParseRSAPrivateKeyPEM(pemBytes []byte) (crypto.Signer, error)
```

Mem-parsing RSA private key berformat PEM PKCS#1 atau PKCS#8. Lewati fungsi
ini jika Anda sudah punya `crypto.Signer` dari tempat lain (klien HSM/KMS,
misalnya).

## Formula string-to-sign

```go
func BuildStringToSignAccessToken(clientID, timestamp string) string
func BuildStringToSignTransaction(method, endpointURL, accessToken string, body []byte, timestamp string, symmetric bool) string
```

Request access-token selalu memakai:

```
clientID + "|" + timestamp
```

Request transaksi memakai salah satu dari dua formula tergantung `symmetric`:

```
symmetric:  HTTPMethod:EndpointUrl:AccessToken:HexSHA256(body):TimeStamp
asymmetric: HTTPMethod:EndpointUrl:HexSHA256(body):TimeStamp
```

`body` harus berupa **bytes persis yang sudah dikirim di wire** —
`BuildStringToSignTransaction` tidak melakukan marshal JSON sendiri. Body
kosong menghasilkan hash digest SHA256 dari byte slice kosong, bukan string
kosong.

<Warning>
  Request access-token selalu ditandatangani secara asymmetric — baik B2B
  maupun B2B2C — terlepas dari mode signing yang disepakati untuk request
  transaksi. Jangan gunakan ulang `ClientSecret`/`SignSymmetric` untuk
  pemanggilan token bahkan pada integrasi symmetric; `TokenManager` sudah
  menangani ini dengan benar untuk Anda.
</Warning>

## Sentinel error

| Sentinel | Dikembalikan oleh | Arti |
|---|---|---|
| `ErrNoPEMBlock` | `ParseRSAPrivateKeyPEM` | input tidak memiliki blok PEM |
| `ErrNotRSAKey` | `ParseRSAPrivateKeyPEM` | blok PEM bukan RSA key |
| `ErrNotRSASigner` | `SignAsymmetric`, `VerifyAsymmetric` | key bukan RSA |
| `ErrWeakRSAKey` | `SignAsymmetric`, `VerifyAsymmetric` | key lebih kecil dari 2048 bit |

Semuanya di-wrap dengan `%w`, jadi cocokkan dengan `errors.Is`.

## Selanjutnya

<CardGroup cols={2}>
  <Card title="Authentication" icon="key" href="/id/concepts/authentication">
    Menggunakan primitif ini untuk mengambil dan menyimpan cache access token B2B/B2B2C.
  </Card>
  <Card title="Verifikasi request inbound" icon="shield-check" href="/id/concepts/webhooks">
    Pasangan sisi server: `ServerVerifier` dan `KeyStore`.
  </Card>
</CardGroup>
