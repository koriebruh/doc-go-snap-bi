---
title: "Direct Debit BI-FAST"
description: "Registrasi e-mandate dan pembayaran BI-FAST, beserta tipe notifikasi masuknya."
---

## Direct Debit BI-FAST

Registrasi e-mandate dan pembayaran BI-FAST, beserta tipe notifikasi masuknya.

<CodeGroup>
```go example.go
resp, err := transferdebit.DirectDebitBIFASTPayment(ctx, transport, hb, transferdebit.DirectDebitBIFASTPaymentRequest{
	PartnerReferenceNo: "2020102900000000000001",
	CustomerReference: "...",
	BeneficiaryAccountNo: "1234567890",
	BeneficiaryAccountName: "Jane Doe",
	TransactionDate: "2026-09-14T10:00:00+07:00",
	BankCode: "...",
	SourceAccountNo: "1234567890",
	SourceAccountName: "Jane Doe",
	EMandateReffID: "2026-09-14T10:00:00+07:00",
})
if err != nil {
	// errors.Is(err, snap.ErrBadRequest), snap.ErrUnauthorized, etc.
}
```
</CodeGroup>

### `DirectDebitBIFASTEMandateRegistration`

DirectDebitBIFASTEMandateRegistration memanggil endpoint SNAP Registrasi e-Mandate (Service Code 70, path .../\{version\}/debit/fast-emandate, HTTP POST — tanpa override method). hb harus sudah membawa setiap field yang dibutuhkan snap.HeaderBuilder kecuali Body, yang diisi sendiri oleh DirectDebitBIFASTEMandateRegistration agar byte hasil marshal yang persis sama dipakai baik untuk signing maupun body di jalur kirim.

Operasi ini tidak idempotent dan package ini tidak melakukan retry. Caller yang me-retry panggilan yang gagal atau timeout sebaiknya memakai ulang X-EXTERNAL-ID yang sama, karena deteksi duplikat di sisi server berpatokan pada header tersebut.

```go
func DirectDebitBIFASTEMandateRegistration(ctx context.Context, t *snap.Transport, hb snap.HeaderBuilder, req DirectDebitBIFASTEMandateRegistrationRequest) (DirectDebitBIFASTEMandateRegistrationResponse, error)
```

**Request &mdash; `DirectDebitBIFASTEMandateRegistrationRequest`**

DirectDebitBIFASTEMandateRegistrationRequest adalah body request untuk API Registrasi e-Mandate (Service Code 70). BankCode, SourceAccountNo, SourceAccountName, BillerID, BillerName, CustomerID, dan ExpiredDatetime bersifat Wajib menurut riset §5.4.

| Field | Type | Presence |
|---|---|---|
| `partnerReferenceNo` | `string` | Opsional |
| `bankCode` | `string` | Wajib |
| `sourceAccountNo` | `string` | Wajib |
| `sourceAccountName` | `string` | Wajib |
| `maxAmount` | `*snap.Money` | Opsional |
| `billerId` | `string` | Wajib |
| `billerName` | `string` | Wajib |
| `customerId` | `string` | Wajib |
| `expiredDatetime` | `string` | Wajib |
| `additionalInfo` | `json.RawMessage` | Opsional |

**Response &mdash; `DirectDebitBIFASTEMandateRegistrationResponse`**

DirectDebitBIFASTEMandateRegistrationResponse adalah body response untuk API Registrasi e-Mandate. ReferenceNo bersifat Kondisional (hanya saat sukses). EMandateReffID bersifat Wajib menurut riset §5.4.

| Field | Type | Presence |
|---|---|---|
| `responseCode` | `string` | Wajib |
| `responseMessage` | `string` | Wajib |
| `referenceNo` | `string` | Opsional |
| `partnerReferenceNo` | `string` | Opsional |
| `eMandateReffId` | `string` | Wajib |
| `additionalInfo` | `json.RawMessage` | Opsional |


---

### `DirectDebitBIFASTPayment`

DirectDebitBIFASTPayment memanggil endpoint SNAP Trigger Direct Debit Transfer / Payment (Service Code 71, path .../\{version\}/debit/fast-payment, HTTP POST — tanpa override method). hb harus sudah membawa setiap field yang dibutuhkan snap.HeaderBuilder kecuali Body, yang diisi sendiri oleh DirectDebitBIFASTPayment agar byte hasil marshal yang persis sama dipakai baik untuk signing maupun body di jalur kirim.

Operasi ini tidak idempotent dan package ini tidak melakukan retry. Caller yang me-retry panggilan yang gagal atau timeout sebaiknya memakai ulang X-EXTERNAL-ID yang sama, karena deteksi duplikat di sisi server berpatokan pada header tersebut.

```go
func DirectDebitBIFASTPayment(ctx context.Context, t *snap.Transport, hb snap.HeaderBuilder, req DirectDebitBIFASTPaymentRequest) (DirectDebitBIFASTPaymentResponse, error)
```

**Request &mdash; `DirectDebitBIFASTPaymentRequest`**

DirectDebitBIFASTPaymentRequest adalah body request untuk API Trigger Direct Debit Transfer / Payment (Service Code 71). SourceAccountNo didokumentasikan String(34) pada tabel field endpoint ini sendiri, berbeda dari String(19) milik Registrasi e-Mandate untuk yang tampaknya merupakan field logis yang sama (riset §6 butir 4) — package ini tidak menerapkan validasi panjang field di mana pun, sehingga perbedaan ini tidak berkonsekuensi pada kode.

| Field | Type | Presence |
|---|---|---|
| `partnerReferenceNo` | `string` | Wajib |
| `currency` | `string` | Opsional |
| `customerReference` | `string` | Wajib |
| `feeType` | `string` | Opsional |
| `remark` | `string` | Opsional |
| `beneficiaryAccountNo` | `string` | Wajib |
| `beneficiaryAccountName` | `string` | Wajib |
| `transactionDate` | `string` | Wajib |
| `bankCode` | `string` | Wajib |
| `sourceAccountNo` | `string` | Wajib |
| `sourceAccountName` | `string` | Wajib |
| `amount` | `*snap.Money` | Opsional |
| `eMandateReffId` | `string` | Wajib |
| `additionalInfo` | `json.RawMessage` | Opsional |

**Response &mdash; `DirectDebitBIFASTPaymentResponse`**

DirectDebitBIFASTPaymentResponse adalah body response untuk API Trigger Direct Debit Transfer / Payment. ReferenceNo bersifat Kondisional (hanya saat sukses). Tidak ada field yang Wajib di luar envelope.

| Field | Type | Presence |
|---|---|---|
| `responseCode` | `string` | Wajib |
| `responseMessage` | `string` | Wajib |
| `referenceNo` | `string` | Opsional |
| `partnerReferenceNo` | `string` | Opsional |
| `additionalInfo` | `json.RawMessage` | Opsional |


---

### `Direct Debit BI-FAST Notification`

<Info>Hanya masuk (inbound) &mdash; package ini tidak memanggil endpoint untuk hal ini. Partner/switcher yang mengirim POST ke callback URL **Anda**; unmarshal body ke `DirectDebitBIFASTNotificationRequest` setelah memverifikasinya dengan `snap.ServerVerifier` (lihat [Memverifikasi request masuk](/id/concepts/webhooks)), lalu balas dengan bentuk di bawah.</Info>

**Diterima &mdash; `DirectDebitBIFASTNotificationRequest`**

DirectDebitBIFASTNotificationRequest adalah body request untuk API Notify (Service Code 72, path .../\{version\}/debit/fast-notify). Ini adalah callback settlement yang diterima PJP, bukan panggilan yang dibuat package ini — tidak ada fungsi pemanggil yang disediakan, mengikuti konvensi baku package ini untuk endpoint notifikasi masuk (QRMPMPaymentNotificationRequest, Fase 23; DirectDebitPaymentNotificationRequest, Fase 27; CPMPaymentNotificationRequest, Fase 29): caller memasang HTTP handler-nya sendiri untuk path ini, mengautentikasi panggilan masuk dengan ServerVerifier.VerifyTransactionRequest, dan melakukan json.Unmarshal pada body ke tipe ini.

OriginalReferenceNo, TransactionStatus, EMandateReffID, SourceAccountNo, dan SourceAccountName bersifat Wajib menurut riset §5.4; setiap field lain Opsional.

Field status ini bernama TransactionStatus, bukan LatestTransactionStatus seperti dipakai di tempat lain baik pada Transfer Kredit maupun sisa Transfer Debit — riset §6 butir 5 mencatat ini sebagai inkonsistensi penamaan yang tidak dijelaskan pada portal itu sendiri dan secara eksplisit menginstruksikan untuk mencatatnya "apa adanya, tidak dinormalisasi diam-diam." Tipe ini mengikuti instruksi tersebut secara harfiah alih-alih mengganti nama field agar cocok dengan sisa package.

| Field | Type | Presence |
|---|---|---|
| `originalReferenceNo` | `string` | Wajib |
| `originalPartnerReferenceNo` | `string` | Opsional |
| `originalExternalId` | `string` | Opsional |
| `transactionStatus` | `string` | Wajib |
| `transactionStatusDesc` | `string` | Opsional |
| `eMandateReffId` | `string` | Wajib |
| `sourceAccountNo` | `string` | Wajib |
| `sourceAccountName` | `string` | Wajib |
| `amount` | `*snap.Money` | Opsional |
| `traceNo` | `string` | Opsional |
| `additionalInfo` | `json.RawMessage` | Opsional |

**Handler Anda membalas dengan &mdash; `DirectDebitBIFASTNotificationResponse`**

DirectDebitBIFASTNotificationResponse adalah body response yang dikirim balik caller untuk API Notify. Riset mendokumentasikan response ini hanya-envelope — tidak ada field selain envelope standar responseCode/responseMessage.

| Field | Type | Presence |
|---|---|---|
| `responseCode` | `string` | Wajib |
| `responseMessage` | `string` | Wajib |
