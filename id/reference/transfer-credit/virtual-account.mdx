---
title: "Virtual Account"
description: "Siklus penuh VA: manajemen (create/update/inquire/delete) dan panggilan sisi transaksi (inquiry, payment, status, varian intrabank, report)."
---

## Virtual Account

Siklus penuh VA: manajemen (create/update/inquire/delete) dan panggilan sisi transaksi (inquiry, payment, status, varian intrabank, report).

<CodeGroup>
```go example.go
resp, err := transfercredit.CreateVA(ctx, transport, hb, transfercredit.CreateVARequest{
	VirtualAccountName: "Jane Doe",
	TrxID: "...",
})
if err != nil {
	// errors.Is(err, snap.ErrBadRequest), snap.ErrUnauthorized, etc.
}
```
</CodeGroup>

### `CreateVA`

CreateVA memanggil endpoint SNAP VA - Create VA endpoint (Service Code 27, path .../\{version\}/transfer-va/create-va). hb harus sudah membawa setiap field yang dibutuhkan snap.HeaderBuilder kecuali Body, yang diisi sendiri oleh CreateVA agar byte hasil marshal yang persis sama dipakai untuk signing maupun wire body.

Operasi ini tidak idempotent dan package ini tidak melakukan retry. Pemanggil yang mengulang panggilan yang gagal atau timeout harus memakai X-EXTERNAL-ID yang sama, karena deteksi duplikat di sisi server berpatokan pada header itu — X-EXTERNAL-ID baru saat retry berisiko membuat VA duplikat.

```go
func CreateVA(ctx context.Context, t *snap.Transport, hb snap.HeaderBuilder, req CreateVARequest) (CreateVAResponse, error)
```

**Request &mdash; `CreateVARequest`**

CreateVARequest adalah request body untuk API VA - Create VA (Service Code 27). VirtualAccountName and TrxID wajib menurut tab Guides; unlike every other Virtual Account endpoint, the identity triple (PartnerServiceID, CustomerNo, VirtualAccountNo) bersifat Opsional here.

| Field | Type | Presence |
|---|---|---|
| `partnerServiceId` | `string` | Opsional |
| `customerNo` | `string` | Opsional |
| `virtualAccountNo` | `string` | Opsional |
| `virtualAccountName` | `string` | Wajib |
| `trxId` | `string` | Wajib |
| `totalAmount` | `*snap.Money` | Opsional |
| `billDetails` | `[]BillDetail` | Opsional |
| `freeTexts` | `[]LocalizedText` | Opsional |
| `virtualAccountTrxType` | `string` | Opsional |
| `feeAmount` | `*snap.Money` | Opsional |
| `expiredDate` | `string` | Opsional |
| `additionalInfo` | `json.RawMessage` | Opsional |

<Accordion title="Field BillDetail">
BillDetail adalah satu entri dalam array "billDetails[]" pada sub-grup Virtual Account (maksimal 24 entri). Semua field tidak ditandai M/O di sumbernya sehingga semua memakai omitempty; BillDescription, BillAmount, dan Reason berupa pointer karena omitempty tidak berpengaruh pada nilai struct non-pointer. BillReferenceNo didokumentasikan Numeric pada tab Guides; sesuai aturan tipe-ambigu package ini, field Numeric yang terdokumentasi diberi tipe json.RawMessage, bukan string, karena field string biasa akan menggagalkan seluruh decode jika penerbit mengirimnya sebagai angka JSON polos, mengikuti presedan AccountTransactionLimit/APIKey yang sudah ada. BillDetail juga dipakai pada request body (CreateVARequest.BillDetails, UpdateVARequest.BillDetails), sehingga pemanggil yang mengisi BillReferenceNo harus menyertakan nilai JSON yang lengkap (mis. json.RawMessage(`"BILLREF1"`) atau json.RawMessage(`123`)), bukan string Go polos — nilai yang tidak lengkap akan menggagalkan json.Marshal untuk seluruh request.

| Field | Type | Presence |
|---|---|---|
| `billCode` | `string` | Opsional |
| `billNo` | `string` | Opsional |
| `billName` | `string` | Opsional |
| `billShortName` | `string` | Opsional |
| `billDescription` | `*LocalizedText` | Opsional |
| `billSubCompany` | `string` | Opsional |
| `billAmount` | `*snap.Money` | Opsional |
| `additionalInfo` | `json.RawMessage` | Opsional |
| `billAmountLabel` | `string` | Opsional |
| `billAmountValue` | `string` | Opsional |
| `billReferenceNo` | `json.RawMessage` | Opsional |
| `status` | `string` | Opsional |
| `reason` | `*LocalizedText` | Opsional |

</Accordion>

<Accordion title="Field LocalizedText">
LocalizedText adalah bentuk teks dwibahasa \{english, indonesia\} bersama yang dipakai di seluruh sub-grup Virtual Account (mis. inquiryReason, paymentFlagReason, billDescription, reason per-bill, entri freeTexts[]). Kedua field bertipe String, tidak ditandai M/O di sumbernya, sehingga keduanya memakai omitempty.

| Field | Type | Presence |
|---|---|---|
| `english` | `string` | Opsional |
| `indonesia` | `string` | Opsional |

</Accordion>

**Response &mdash; `CreateVAResponse`**

CreateVAResponse adalah response body untuk API VA - Create VA.

| Field | Type | Presence |
|---|---|---|
| `responseCode` | `string` | Wajib |
| `responseMessage` | `string` | Wajib |
| `virtualAccountData` | `*CreateVAData` | Opsional |

<Accordion title="Field CreateVAData">
CreateVAData is the "virtualAccountData" object in CreateVAResponse.

| Field | Type | Presence |
|---|---|---|
| `partnerServiceId` | `string` | Opsional |
| `customerNo` | `string` | Opsional |
| `virtualAccountNo` | `string` | Opsional |
| `virtualAccountName` | `string` | Opsional |
| `trxId` | `string` | Opsional |
| `totalAmount` | `*snap.Money` | Opsional |
| `billDetails` | `[]BillDetail` | Opsional |
| `freeTexts` | `[]LocalizedText` | Opsional |
| `virtualAccountTrxType` | `string` | Opsional |
| `feeAmount` | `*snap.Money` | Opsional |
| `expiredDate` | `string` | Opsional |
| `additionalInfo` | `json.RawMessage` | Opsional |

</Accordion>

<Accordion title="Field BillDetail">
BillDetail adalah satu entri dalam array "billDetails[]" pada sub-grup Virtual Account (maksimal 24 entri). Semua field tidak ditandai M/O di sumbernya sehingga semua memakai omitempty; BillDescription, BillAmount, dan Reason berupa pointer karena omitempty tidak berpengaruh pada nilai struct non-pointer. BillReferenceNo didokumentasikan Numeric pada tab Guides; sesuai aturan tipe-ambigu package ini, field Numeric yang terdokumentasi diberi tipe json.RawMessage, bukan string, karena field string biasa akan menggagalkan seluruh decode jika penerbit mengirimnya sebagai angka JSON polos, mengikuti presedan AccountTransactionLimit/APIKey yang sudah ada. BillDetail juga dipakai pada request body (CreateVARequest.BillDetails, UpdateVARequest.BillDetails), sehingga pemanggil yang mengisi BillReferenceNo harus menyertakan nilai JSON yang lengkap (mis. json.RawMessage(`"BILLREF1"`) atau json.RawMessage(`123`)), bukan string Go polos — nilai yang tidak lengkap akan menggagalkan json.Marshal untuk seluruh request.

| Field | Type | Presence |
|---|---|---|
| `billCode` | `string` | Opsional |
| `billNo` | `string` | Opsional |
| `billName` | `string` | Opsional |
| `billShortName` | `string` | Opsional |
| `billDescription` | `*LocalizedText` | Opsional |
| `billSubCompany` | `string` | Opsional |
| `billAmount` | `*snap.Money` | Opsional |
| `additionalInfo` | `json.RawMessage` | Opsional |
| `billAmountLabel` | `string` | Opsional |
| `billAmountValue` | `string` | Opsional |
| `billReferenceNo` | `json.RawMessage` | Opsional |
| `status` | `string` | Opsional |
| `reason` | `*LocalizedText` | Opsional |

</Accordion>

<Accordion title="Field LocalizedText">
LocalizedText adalah bentuk teks dwibahasa \{english, indonesia\} bersama yang dipakai di seluruh sub-grup Virtual Account (mis. inquiryReason, paymentFlagReason, billDescription, reason per-bill, entri freeTexts[]). Kedua field bertipe String, tidak ditandai M/O di sumbernya, sehingga keduanya memakai omitempty.

| Field | Type | Presence |
|---|---|---|
| `english` | `string` | Opsional |
| `indonesia` | `string` | Opsional |

</Accordion>


---

### `UpdateVA`

UpdateVA memanggil endpoint SNAP VA - Update VA (Service Code 28, path .../\{version\}/transfer-va/update-va, HTTP PUT). hb harus sudah membawa setiap field yang dibutuhkan snap.HeaderBuilder kecuali Method dan Body, yang diisi sendiri oleh UpdateVA: Method menjadi PUT (method sudah ditetapkan oleh endpoint ini, tidak bisa dikonfigurasi pemanggil) dan Body, agar byte hasil marshal yang persis sama dipakai untuk signing maupun wire body.

Operasi ini tidak idempotent dan package ini tidak melakukan retry. Pemanggil yang mengulang panggilan yang gagal atau timeout harus memakai X-EXTERNAL-ID yang sama, karena deteksi duplikat di sisi server berpatokan pada header itu.

```go
func UpdateVA(ctx context.Context, t *snap.Transport, hb snap.HeaderBuilder, req UpdateVARequest) (UpdateVAResponse, error)
```

**Request &mdash; `UpdateVARequest`**

UpdateVARequest adalah request body untuk API VA - Update VA (Service Code 28). PartnerServiceID, CustomerNo, VirtualAccountNo, VirtualAccountName, and TrxID wajib menurut tab Guides — berbeda dari Create VA, trio identitas ini Wajib.

| Field | Type | Presence |
|---|---|---|
| `partnerServiceId` | `string` | Wajib |
| `customerNo` | `string` | Wajib |
| `virtualAccountNo` | `string` | Wajib |
| `virtualAccountName` | `string` | Wajib |
| `trxId` | `string` | Wajib |
| `totalAmount` | `*snap.Money` | Opsional |
| `billDetails` | `[]BillDetail` | Opsional |
| `freeTexts` | `[]LocalizedText` | Opsional |
| `virtualAccountTrxType` | `string` | Opsional |
| `feeAmount` | `*snap.Money` | Opsional |
| `expiredDate` | `string` | Opsional |
| `additionalInfo` | `json.RawMessage` | Opsional |

<Accordion title="Field BillDetail">
BillDetail adalah satu entri dalam array "billDetails[]" pada sub-grup Virtual Account (maksimal 24 entri). Semua field tidak ditandai M/O di sumbernya sehingga semua memakai omitempty; BillDescription, BillAmount, dan Reason berupa pointer karena omitempty tidak berpengaruh pada nilai struct non-pointer. BillReferenceNo didokumentasikan Numeric pada tab Guides; sesuai aturan tipe-ambigu package ini, field Numeric yang terdokumentasi diberi tipe json.RawMessage, bukan string, karena field string biasa akan menggagalkan seluruh decode jika penerbit mengirimnya sebagai angka JSON polos, mengikuti presedan AccountTransactionLimit/APIKey yang sudah ada. BillDetail juga dipakai pada request body (CreateVARequest.BillDetails, UpdateVARequest.BillDetails), sehingga pemanggil yang mengisi BillReferenceNo harus menyertakan nilai JSON yang lengkap (mis. json.RawMessage(`"BILLREF1"`) atau json.RawMessage(`123`)), bukan string Go polos — nilai yang tidak lengkap akan menggagalkan json.Marshal untuk seluruh request.

| Field | Type | Presence |
|---|---|---|
| `billCode` | `string` | Opsional |
| `billNo` | `string` | Opsional |
| `billName` | `string` | Opsional |
| `billShortName` | `string` | Opsional |
| `billDescription` | `*LocalizedText` | Opsional |
| `billSubCompany` | `string` | Opsional |
| `billAmount` | `*snap.Money` | Opsional |
| `additionalInfo` | `json.RawMessage` | Opsional |
| `billAmountLabel` | `string` | Opsional |
| `billAmountValue` | `string` | Opsional |
| `billReferenceNo` | `json.RawMessage` | Opsional |
| `status` | `string` | Opsional |
| `reason` | `*LocalizedText` | Opsional |

</Accordion>

<Accordion title="Field LocalizedText">
LocalizedText adalah bentuk teks dwibahasa \{english, indonesia\} bersama yang dipakai di seluruh sub-grup Virtual Account (mis. inquiryReason, paymentFlagReason, billDescription, reason per-bill, entri freeTexts[]). Kedua field bertipe String, tidak ditandai M/O di sumbernya, sehingga keduanya memakai omitempty.

| Field | Type | Presence |
|---|---|---|
| `english` | `string` | Opsional |
| `indonesia` | `string` | Opsional |

</Accordion>

**Response &mdash; `UpdateVAResponse`**

UpdateVAResponse adalah response body untuk API VA - Update VA.

| Field | Type | Presence |
|---|---|---|
| `responseCode` | `string` | Wajib |
| `responseMessage` | `string` | Wajib |
| `virtualAccountData` | `*UpdateVAData` | Opsional |

<Accordion title="Field UpdateVAData">
UpdateVAData is the "virtualAccountData" object in UpdateVAResponse — Create VA's data fields plus LastUpdateDate and PaymentDate.

| Field | Type | Presence |
|---|---|---|
| `partnerServiceId` | `string` | Opsional |
| `customerNo` | `string` | Opsional |
| `virtualAccountNo` | `string` | Opsional |
| `virtualAccountName` | `string` | Opsional |
| `trxId` | `string` | Opsional |
| `totalAmount` | `*snap.Money` | Opsional |
| `billDetails` | `[]BillDetail` | Opsional |
| `freeTexts` | `[]LocalizedText` | Opsional |
| `virtualAccountTrxType` | `string` | Opsional |
| `feeAmount` | `*snap.Money` | Opsional |
| `expiredDate` | `string` | Opsional |
| `lastUpdateDate` | `string` | Opsional |
| `paymentDate` | `string` | Opsional |
| `additionalInfo` | `json.RawMessage` | Opsional |

</Accordion>

<Accordion title="Field BillDetail">
BillDetail adalah satu entri dalam array "billDetails[]" pada sub-grup Virtual Account (maksimal 24 entri). Semua field tidak ditandai M/O di sumbernya sehingga semua memakai omitempty; BillDescription, BillAmount, dan Reason berupa pointer karena omitempty tidak berpengaruh pada nilai struct non-pointer. BillReferenceNo didokumentasikan Numeric pada tab Guides; sesuai aturan tipe-ambigu package ini, field Numeric yang terdokumentasi diberi tipe json.RawMessage, bukan string, karena field string biasa akan menggagalkan seluruh decode jika penerbit mengirimnya sebagai angka JSON polos, mengikuti presedan AccountTransactionLimit/APIKey yang sudah ada. BillDetail juga dipakai pada request body (CreateVARequest.BillDetails, UpdateVARequest.BillDetails), sehingga pemanggil yang mengisi BillReferenceNo harus menyertakan nilai JSON yang lengkap (mis. json.RawMessage(`"BILLREF1"`) atau json.RawMessage(`123`)), bukan string Go polos — nilai yang tidak lengkap akan menggagalkan json.Marshal untuk seluruh request.

| Field | Type | Presence |
|---|---|---|
| `billCode` | `string` | Opsional |
| `billNo` | `string` | Opsional |
| `billName` | `string` | Opsional |
| `billShortName` | `string` | Opsional |
| `billDescription` | `*LocalizedText` | Opsional |
| `billSubCompany` | `string` | Opsional |
| `billAmount` | `*snap.Money` | Opsional |
| `additionalInfo` | `json.RawMessage` | Opsional |
| `billAmountLabel` | `string` | Opsional |
| `billAmountValue` | `string` | Opsional |
| `billReferenceNo` | `json.RawMessage` | Opsional |
| `status` | `string` | Opsional |
| `reason` | `*LocalizedText` | Opsional |

</Accordion>

<Accordion title="Field LocalizedText">
LocalizedText adalah bentuk teks dwibahasa \{english, indonesia\} bersama yang dipakai di seluruh sub-grup Virtual Account (mis. inquiryReason, paymentFlagReason, billDescription, reason per-bill, entri freeTexts[]). Kedua field bertipe String, tidak ditandai M/O di sumbernya, sehingga keduanya memakai omitempty.

| Field | Type | Presence |
|---|---|---|
| `english` | `string` | Opsional |
| `indonesia` | `string` | Opsional |

</Accordion>


---

### `UpdateStatusVA`

UpdateStatusVA memanggil endpoint SNAP VA - Update Status VA (Service Code 29, path .../\{version\}/transfer-va/update-status, HTTP PUT). hb harus sudah membawa setiap field yang dibutuhkan snap.HeaderBuilder kecuali Method dan Body, yang diisi sendiri oleh UpdateStatusVA: Method menjadi PUT (method sudah ditetapkan oleh endpoint ini, tidak bisa dikonfigurasi pemanggil) dan Body, agar byte hasil marshal yang persis sama dipakai untuk signing maupun wire body.

Operasi ini tidak idempotent dan package ini tidak melakukan retry. Pemanggil yang mengulang panggilan yang gagal atau timeout harus memakai X-EXTERNAL-ID yang sama, karena deteksi duplikat di sisi server berpatokan pada header itu.

```go
func UpdateStatusVA(ctx context.Context, t *snap.Transport, hb snap.HeaderBuilder, req UpdateStatusVARequest) (UpdateStatusVAResponse, error)
```

**Request &mdash; `UpdateStatusVARequest`**

UpdateStatusVARequest adalah request body untuk API VA - Update Status VA (Service Code 29). PartnerServiceID, CustomerNo, VirtualAccountNo, TrxID, and PaidStatus wajib menurut tab Guides. Nilai PaidStatus adalah "Y"/"N".

| Field | Type | Presence |
|---|---|---|
| `partnerServiceId` | `string` | Wajib |
| `customerNo` | `string` | Wajib |
| `virtualAccountNo` | `string` | Wajib |
| `trxId` | `string` | Wajib |
| `paidStatus` | `string` | Wajib |
| `additionalInfo` | `json.RawMessage` | Opsional |

**Response &mdash; `UpdateStatusVAResponse`**

UpdateStatusVAResponse adalah response body untuk API VA - Update Status VA.

| Field | Type | Presence |
|---|---|---|
| `responseCode` | `string` | Wajib |
| `responseMessage` | `string` | Wajib |
| `virtualAccountData` | `*UpdateStatusVAData` | Opsional |

<Accordion title="Field UpdateStatusVAData">
UpdateStatusVAData adalah objek "virtualAccountData" pada UpdateStatusVAResponse — "objek data VA lengkap", set field yang sama dengan UpdateVAData, dengan nama tipenya sendiri.

| Field | Type | Presence |
|---|---|---|
| `partnerServiceId` | `string` | Opsional |
| `customerNo` | `string` | Opsional |
| `virtualAccountNo` | `string` | Opsional |
| `virtualAccountName` | `string` | Opsional |
| `trxId` | `string` | Opsional |
| `totalAmount` | `*snap.Money` | Opsional |
| `billDetails` | `[]BillDetail` | Opsional |
| `freeTexts` | `[]LocalizedText` | Opsional |
| `virtualAccountTrxType` | `string` | Opsional |
| `feeAmount` | `*snap.Money` | Opsional |
| `expiredDate` | `string` | Opsional |
| `lastUpdateDate` | `string` | Opsional |
| `paymentDate` | `string` | Opsional |
| `additionalInfo` | `json.RawMessage` | Opsional |

</Accordion>

<Accordion title="Field BillDetail">
BillDetail adalah satu entri dalam array "billDetails[]" pada sub-grup Virtual Account (maksimal 24 entri). Semua field tidak ditandai M/O di sumbernya sehingga semua memakai omitempty; BillDescription, BillAmount, dan Reason berupa pointer karena omitempty tidak berpengaruh pada nilai struct non-pointer. BillReferenceNo didokumentasikan Numeric pada tab Guides; sesuai aturan tipe-ambigu package ini, field Numeric yang terdokumentasi diberi tipe json.RawMessage, bukan string, karena field string biasa akan menggagalkan seluruh decode jika penerbit mengirimnya sebagai angka JSON polos, mengikuti presedan AccountTransactionLimit/APIKey yang sudah ada. BillDetail juga dipakai pada request body (CreateVARequest.BillDetails, UpdateVARequest.BillDetails), sehingga pemanggil yang mengisi BillReferenceNo harus menyertakan nilai JSON yang lengkap (mis. json.RawMessage(`"BILLREF1"`) atau json.RawMessage(`123`)), bukan string Go polos — nilai yang tidak lengkap akan menggagalkan json.Marshal untuk seluruh request.

| Field | Type | Presence |
|---|---|---|
| `billCode` | `string` | Opsional |
| `billNo` | `string` | Opsional |
| `billName` | `string` | Opsional |
| `billShortName` | `string` | Opsional |
| `billDescription` | `*LocalizedText` | Opsional |
| `billSubCompany` | `string` | Opsional |
| `billAmount` | `*snap.Money` | Opsional |
| `additionalInfo` | `json.RawMessage` | Opsional |
| `billAmountLabel` | `string` | Opsional |
| `billAmountValue` | `string` | Opsional |
| `billReferenceNo` | `json.RawMessage` | Opsional |
| `status` | `string` | Opsional |
| `reason` | `*LocalizedText` | Opsional |

</Accordion>

<Accordion title="Field LocalizedText">
LocalizedText adalah bentuk teks dwibahasa \{english, indonesia\} bersama yang dipakai di seluruh sub-grup Virtual Account (mis. inquiryReason, paymentFlagReason, billDescription, reason per-bill, entri freeTexts[]). Kedua field bertipe String, tidak ditandai M/O di sumbernya, sehingga keduanya memakai omitempty.

| Field | Type | Presence |
|---|---|---|
| `english` | `string` | Opsional |
| `indonesia` | `string` | Opsional |

</Accordion>


---

### `InquiryVA`

InquiryVA memanggil endpoint SNAP VA - Inquiry VA endpoint (Service Code 30, path .../\{version\}/transfer-va/inquiry-va). hb harus sudah membawa setiap field yang dibutuhkan snap.HeaderBuilder kecuali Body, yang diisi sendiri oleh InquiryVA agar byte hasil marshal yang persis sama dipakai untuk signing maupun wire body.

```go
func InquiryVA(ctx context.Context, t *snap.Transport, hb snap.HeaderBuilder, req InquiryVARequest) (InquiryVAResponse, error)
```

**Request &mdash; `InquiryVARequest`**

InquiryVARequest adalah request body untuk API VA - Inquiry VA (Service Code 30). PartnerServiceID, CustomerNo, VirtualAccountNo, and TrxID wajib menurut tab Guides.

| Field | Type | Presence |
|---|---|---|
| `partnerServiceId` | `string` | Wajib |
| `customerNo` | `string` | Wajib |
| `virtualAccountNo` | `string` | Wajib |
| `trxId` | `string` | Wajib |
| `additionalInfo` | `json.RawMessage` | Opsional |

**Response &mdash; `InquiryVAResponse`**

InquiryVAResponse adalah response body untuk API VA - Inquiry VA.

| Field | Type | Presence |
|---|---|---|
| `responseCode` | `string` | Wajib |
| `responseMessage` | `string` | Wajib |
| `virtualAccountData` | `*InquiryVAData` | Opsional |

<Accordion title="Field InquiryVAData">
InquiryVAData adalah objek "virtualAccountData" pada InquiryVAResponse — "objek data VA lengkap (bentuk sama dengan response Update VA)", set field yang sama dengan UpdateVAData, dengan nama tipenya sendiri.

| Field | Type | Presence |
|---|---|---|
| `partnerServiceId` | `string` | Opsional |
| `customerNo` | `string` | Opsional |
| `virtualAccountNo` | `string` | Opsional |
| `virtualAccountName` | `string` | Opsional |
| `trxId` | `string` | Opsional |
| `totalAmount` | `*snap.Money` | Opsional |
| `billDetails` | `[]BillDetail` | Opsional |
| `freeTexts` | `[]LocalizedText` | Opsional |
| `virtualAccountTrxType` | `string` | Opsional |
| `feeAmount` | `*snap.Money` | Opsional |
| `expiredDate` | `string` | Opsional |
| `lastUpdateDate` | `string` | Opsional |
| `paymentDate` | `string` | Opsional |
| `additionalInfo` | `json.RawMessage` | Opsional |

</Accordion>

<Accordion title="Field BillDetail">
BillDetail adalah satu entri dalam array "billDetails[]" pada sub-grup Virtual Account (maksimal 24 entri). Semua field tidak ditandai M/O di sumbernya sehingga semua memakai omitempty; BillDescription, BillAmount, dan Reason berupa pointer karena omitempty tidak berpengaruh pada nilai struct non-pointer. BillReferenceNo didokumentasikan Numeric pada tab Guides; sesuai aturan tipe-ambigu package ini, field Numeric yang terdokumentasi diberi tipe json.RawMessage, bukan string, karena field string biasa akan menggagalkan seluruh decode jika penerbit mengirimnya sebagai angka JSON polos, mengikuti presedan AccountTransactionLimit/APIKey yang sudah ada. BillDetail juga dipakai pada request body (CreateVARequest.BillDetails, UpdateVARequest.BillDetails), sehingga pemanggil yang mengisi BillReferenceNo harus menyertakan nilai JSON yang lengkap (mis. json.RawMessage(`"BILLREF1"`) atau json.RawMessage(`123`)), bukan string Go polos — nilai yang tidak lengkap akan menggagalkan json.Marshal untuk seluruh request.

| Field | Type | Presence |
|---|---|---|
| `billCode` | `string` | Opsional |
| `billNo` | `string` | Opsional |
| `billName` | `string` | Opsional |
| `billShortName` | `string` | Opsional |
| `billDescription` | `*LocalizedText` | Opsional |
| `billSubCompany` | `string` | Opsional |
| `billAmount` | `*snap.Money` | Opsional |
| `additionalInfo` | `json.RawMessage` | Opsional |
| `billAmountLabel` | `string` | Opsional |
| `billAmountValue` | `string` | Opsional |
| `billReferenceNo` | `json.RawMessage` | Opsional |
| `status` | `string` | Opsional |
| `reason` | `*LocalizedText` | Opsional |

</Accordion>

<Accordion title="Field LocalizedText">
LocalizedText adalah bentuk teks dwibahasa \{english, indonesia\} bersama yang dipakai di seluruh sub-grup Virtual Account (mis. inquiryReason, paymentFlagReason, billDescription, reason per-bill, entri freeTexts[]). Kedua field bertipe String, tidak ditandai M/O di sumbernya, sehingga keduanya memakai omitempty.

| Field | Type | Presence |
|---|---|---|
| `english` | `string` | Opsional |
| `indonesia` | `string` | Opsional |

</Accordion>


---

### `DeleteVA`

DeleteVA memanggil endpoint SNAP VA - Delete VA endpoint (Service Code 31, path .../\{version\}/transfer-va/delete-va, HTTP DELETE). hb harus sudah membawa setiap field yang dibutuhkan snap.HeaderBuilder kecuali Method dan Body, yang diisi sendiri oleh DeleteVA: Method menjadi DELETE (method sudah ditetapkan oleh endpoint ini, tidak bisa dikonfigurasi pemanggil) dan Body agar byte hasil marshal yang persis sama dipakai untuk signing maupun wire body.

Operasi ini tidak idempotent dan package ini tidak melakukan retry. Pemanggil yang mengulang panggilan yang gagal atau timeout harus memakai X-EXTERNAL-ID yang sama, karena deteksi duplikat di sisi server berpatokan pada header itu.

```go
func DeleteVA(ctx context.Context, t *snap.Transport, hb snap.HeaderBuilder, req DeleteVARequest) (DeleteVAResponse, error)
```

**Request &mdash; `DeleteVARequest`**

DeleteVARequest adalah request body untuk API VA - Delete VA (Service Code 31). PartnerServiceID, CustomerNo, and VirtualAccountNo wajib menurut tab Guides; TrxID bersifat Opsional.

| Field | Type | Presence |
|---|---|---|
| `partnerServiceId` | `string` | Wajib |
| `customerNo` | `string` | Wajib |
| `virtualAccountNo` | `string` | Wajib |
| `trxId` | `string` | Opsional |
| `additionalInfo` | `json.RawMessage` | Opsional |

**Response &mdash; `DeleteVAResponse`**

DeleteVAResponse adalah response body untuk API VA - Delete VA.

| Field | Type | Presence |
|---|---|---|
| `responseCode` | `string` | Wajib |
| `responseMessage` | `string` | Wajib |
| `virtualAccountData` | `*DeleteVAData` | Opsional |

<Accordion title="Field DeleteVAData">
DeleteVAData adalah objek "virtualAccountData" pada DeleteVAResponse — bentuknya lebih kecil dibanding objek data endpoint manajemen VA lainnya.

| Field | Type | Presence |
|---|---|---|
| `partnerServiceId` | `string` | Opsional |
| `customerNo` | `string` | Opsional |
| `virtualAccountNo` | `string` | Opsional |
| `trxId` | `string` | Opsional |
| `additionalInfo` | `json.RawMessage` | Opsional |

</Accordion>


---

### `VAInquiry`

VAInquiry memanggil endpoint SNAP VA - VA Inquiry (Service Code 24, path .../\{version\}/transfer-va/inquiry, HTTP POST — bukan PUT/DELETE seperti endpoint manajemen VA lainnya). hb harus sudah membawa setiap field yang dibutuhkan snap.HeaderBuilder kecuali Body, yang diisi sendiri oleh VAInquiry agar byte hasil marshal yang persis sama dipakai untuk signing maupun wire body.

```go
func VAInquiry(ctx context.Context, t *snap.Transport, hb snap.HeaderBuilder, req VAInquiryRequest) (VAInquiryResponse, error)
```

**Request &mdash; `VAInquiryRequest`**

VAInquiryRequest adalah request body untuk API VA - VA Inquiry (Service Code 24). PartnerServiceID, CustomerNo, VirtualAccountNo, dan InquiryRequestID wajib menurut tab Guides. CustomerNo bertipe json.RawMessage, bukan string: field ini terdokumentasi String(20) tetapi muncul sebagai angka JSON polos pada contoh endpoint sejenisnya, VA Inquiry Status (26). Pemanggil yang mengisi CustomerNo atau ChannelCode harus menyertakan nilai JSON yang lengkap (mis. json.RawMessage(`"98765"`) atau json.RawMessage(`98765`)), bukan string Go polos.

| Field | Type | Presence |
|---|---|---|
| `partnerServiceId` | `string` | Wajib |
| `customerNo` | `json.RawMessage` | Wajib |
| `virtualAccountNo` | `string` | Wajib |
| `trxDateInit` | `string` | Opsional |
| `channelCode` | `json.RawMessage` | Opsional |
| `language` | `string` | Opsional |
| `hashedSourceAccountNo` | `string` | Opsional |
| `sourceBankCode` | `string` | Opsional |
| `passApp` | `string` | Opsional |
| `inquiryRequestId` | `string` | Wajib |
| `additionalInfo` | `json.RawMessage` | Opsional |

**Response &mdash; `VAInquiryResponse`**

VAInquiryResponse adalah response body untuk API VA - VA Inquiry.

| Field | Type | Presence |
|---|---|---|
| `responseCode` | `string` | Wajib |
| `responseMessage` | `string` | Wajib |
| `virtualAccountData` | `*VAInquiryData` | Opsional |

<Accordion title="Field VAInquiryData">
VAInquiryData adalah objek "virtualAccountData" pada VAInquiryResponse. Field di luar trio identitas tidak diberi tanda M/O pada tab Guides, sehingga semuanya Opsional (omitempty); InquiryReason, TotalAmount, dan FeeAmount berupa pointer karena omitempty tidak berpengaruh pada nilai struct non-pointer.

| Field | Type | Presence |
|---|---|---|
| `partnerServiceId` | `string` | Opsional |
| `customerNo` | `json.RawMessage` | Opsional |
| `virtualAccountNo` | `string` | Opsional |
| `inquiryStatus` | `string` | Opsional |
| `inquiryReason` | `*LocalizedText` | Opsional |
| `virtualAccountName` | `string` | Opsional |
| `virtualAccountEmail` | `string` | Opsional |
| `virtualAccountPhone` | `string` | Opsional |
| `inquiryRequestId` | `string` | Opsional |
| `totalAmount` | `*snap.Money` | Opsional |
| `subCompany` | `string` | Opsional |
| `billDetails` | `[]BillDetail` | Opsional |
| `freeTexts` | `[]LocalizedText` | Opsional |
| `virtualAccountTrxType` | `string` | Opsional |
| `feeAmount` | `*snap.Money` | Opsional |
| `additionalInfo` | `json.RawMessage` | Opsional |

</Accordion>

<Accordion title="Field LocalizedText">
LocalizedText adalah bentuk teks dwibahasa \{english, indonesia\} bersama yang dipakai di seluruh sub-grup Virtual Account (mis. inquiryReason, paymentFlagReason, billDescription, reason per-bill, entri freeTexts[]). Kedua field bertipe String, tidak ditandai M/O di sumbernya, sehingga keduanya memakai omitempty.

| Field | Type | Presence |
|---|---|---|
| `english` | `string` | Opsional |
| `indonesia` | `string` | Opsional |

</Accordion>

<Accordion title="Field BillDetail">
BillDetail adalah satu entri dalam array "billDetails[]" pada sub-grup Virtual Account (maksimal 24 entri). Semua field tidak ditandai M/O di sumbernya sehingga semua memakai omitempty; BillDescription, BillAmount, dan Reason berupa pointer karena omitempty tidak berpengaruh pada nilai struct non-pointer. BillReferenceNo didokumentasikan Numeric pada tab Guides; sesuai aturan tipe-ambigu package ini, field Numeric yang terdokumentasi diberi tipe json.RawMessage, bukan string, karena field string biasa akan menggagalkan seluruh decode jika penerbit mengirimnya sebagai angka JSON polos, mengikuti presedan AccountTransactionLimit/APIKey yang sudah ada. BillDetail juga dipakai pada request body (CreateVARequest.BillDetails, UpdateVARequest.BillDetails), sehingga pemanggil yang mengisi BillReferenceNo harus menyertakan nilai JSON yang lengkap (mis. json.RawMessage(`"BILLREF1"`) atau json.RawMessage(`123`)), bukan string Go polos — nilai yang tidak lengkap akan menggagalkan json.Marshal untuk seluruh request.

| Field | Type | Presence |
|---|---|---|
| `billCode` | `string` | Opsional |
| `billNo` | `string` | Opsional |
| `billName` | `string` | Opsional |
| `billShortName` | `string` | Opsional |
| `billDescription` | `*LocalizedText` | Opsional |
| `billSubCompany` | `string` | Opsional |
| `billAmount` | `*snap.Money` | Opsional |
| `additionalInfo` | `json.RawMessage` | Opsional |
| `billAmountLabel` | `string` | Opsional |
| `billAmountValue` | `string` | Opsional |
| `billReferenceNo` | `json.RawMessage` | Opsional |
| `status` | `string` | Opsional |
| `reason` | `*LocalizedText` | Opsional |

</Accordion>


---

### `VAPayment`

VAPayment memanggil endpoint SNAP VA - VA Payment endpoint (Service Code 25, path .../\{version\}/transfer-va/payment, HTTP POST — no method override). hb harus sudah membawa setiap field yang dibutuhkan snap.HeaderBuilder kecuali Body, yang diisi sendiri oleh VAPayment agar byte hasil marshal yang persis sama dipakai untuk signing maupun wire body.

Operasi ini tidak idempotent dan package ini tidak melakukan retry. Pemanggil yang mengulang panggilan yang gagal atau timeout harus memakai X-EXTERNAL-ID yang sama, karena deteksi duplikat di sisi server berpatokan pada header itu.

```go
func VAPayment(ctx context.Context, t *snap.Transport, hb snap.HeaderBuilder, req VAPaymentRequest) (VAPaymentResponse, error)
```

**Request &mdash; `VAPaymentRequest`**

VAPaymentRequest adalah request body untuk API VA - VA Payment (Service Code 25). PartnerServiceID, CustomerNo, VirtualAccountNo, PaymentRequestID, dan PaidAmount wajib menurut tab Guides. TrxID bersifat Kondisional — "wajib jika berasal dari Create VA", sebuah kondisi bergantung-data yang tidak bisa dinyatakan oleh sistem tipe, sehingga di sini dibuat Opsional (omitempty). CustomerNo, ChannelCode, dan PaymentType bertipe json.RawMessage (PaymentType terdokumentasi String(1) tetapi setiap contoh selalu menampilkan angka polos).

| Field | Type | Presence |
|---|---|---|
| `partnerServiceId` | `string` | Wajib |
| `customerNo` | `json.RawMessage` | Wajib |
| `virtualAccountNo` | `string` | Wajib |
| `trxId` | `string` | Opsional |
| `paymentRequestId` | `string` | Wajib |
| `channelCode` | `json.RawMessage` | Opsional |
| `hashedSourceAccountNo` | `string` | Opsional |
| `sourceBankCode` | `string` | Opsional |
| `paidAmount` | `snap.Money` | Wajib |
| `cumulativePaymentAmount` | `*snap.Money` | Opsional |
| `paidBills` | `string` | Opsional |
| `totalAmount` | `*snap.Money` | Opsional |
| `trxDateTime` | `string` | Opsional |
| `referenceNo` | `string` | Opsional |
| `journalNum` | `string` | Opsional |
| `paymentType` | `json.RawMessage` | Opsional |
| `flagAdvise` | `string` | Opsional |
| `subCompany` | `string` | Opsional |
| `billDetails` | `[]BillDetail` | Opsional |
| `freeTexts` | `[]LocalizedText` | Opsional |
| `additionalInfo` | `json.RawMessage` | Opsional |

<Accordion title="Field BillDetail">
BillDetail adalah satu entri dalam array "billDetails[]" pada sub-grup Virtual Account (maksimal 24 entri). Semua field tidak ditandai M/O di sumbernya sehingga semua memakai omitempty; BillDescription, BillAmount, dan Reason berupa pointer karena omitempty tidak berpengaruh pada nilai struct non-pointer. BillReferenceNo didokumentasikan Numeric pada tab Guides; sesuai aturan tipe-ambigu package ini, field Numeric yang terdokumentasi diberi tipe json.RawMessage, bukan string, karena field string biasa akan menggagalkan seluruh decode jika penerbit mengirimnya sebagai angka JSON polos, mengikuti presedan AccountTransactionLimit/APIKey yang sudah ada. BillDetail juga dipakai pada request body (CreateVARequest.BillDetails, UpdateVARequest.BillDetails), sehingga pemanggil yang mengisi BillReferenceNo harus menyertakan nilai JSON yang lengkap (mis. json.RawMessage(`"BILLREF1"`) atau json.RawMessage(`123`)), bukan string Go polos — nilai yang tidak lengkap akan menggagalkan json.Marshal untuk seluruh request.

| Field | Type | Presence |
|---|---|---|
| `billCode` | `string` | Opsional |
| `billNo` | `string` | Opsional |
| `billName` | `string` | Opsional |
| `billShortName` | `string` | Opsional |
| `billDescription` | `*LocalizedText` | Opsional |
| `billSubCompany` | `string` | Opsional |
| `billAmount` | `*snap.Money` | Opsional |
| `additionalInfo` | `json.RawMessage` | Opsional |
| `billAmountLabel` | `string` | Opsional |
| `billAmountValue` | `string` | Opsional |
| `billReferenceNo` | `json.RawMessage` | Opsional |
| `status` | `string` | Opsional |
| `reason` | `*LocalizedText` | Opsional |

</Accordion>

<Accordion title="Field LocalizedText">
LocalizedText adalah bentuk teks dwibahasa \{english, indonesia\} bersama yang dipakai di seluruh sub-grup Virtual Account (mis. inquiryReason, paymentFlagReason, billDescription, reason per-bill, entri freeTexts[]). Kedua field bertipe String, tidak ditandai M/O di sumbernya, sehingga keduanya memakai omitempty.

| Field | Type | Presence |
|---|---|---|
| `english` | `string` | Opsional |
| `indonesia` | `string` | Opsional |

</Accordion>

**Response &mdash; `VAPaymentResponse`**

VAPaymentResponse adalah response body untuk API VA - VA Payment.

| Field | Type | Presence |
|---|---|---|
| `responseCode` | `string` | Wajib |
| `responseMessage` | `string` | Wajib |
| `virtualAccountData` | `*VAPaymentData` | Opsional |

<Accordion title="Field VAPaymentData">
VAPaymentData adalah objek "virtualAccountData" pada VAPaymentResponse — response ini mencerminkan seluruh field request ditambah PaymentFlagReason dan PaymentFlagStatus. Status/reason per-bill tidak butuh field baru: BillDetail sudah membawa Status dan Reason.

| Field | Type | Presence |
|---|---|---|
| `partnerServiceId` | `string` | Opsional |
| `customerNo` | `json.RawMessage` | Opsional |
| `virtualAccountNo` | `string` | Opsional |
| `trxId` | `string` | Opsional |
| `paymentRequestId` | `string` | Opsional |
| `channelCode` | `json.RawMessage` | Opsional |
| `hashedSourceAccountNo` | `string` | Opsional |
| `sourceBankCode` | `string` | Opsional |
| `paidAmount` | `*snap.Money` | Opsional |
| `cumulativePaymentAmount` | `*snap.Money` | Opsional |
| `paidBills` | `string` | Opsional |
| `totalAmount` | `*snap.Money` | Opsional |
| `trxDateTime` | `string` | Opsional |
| `referenceNo` | `string` | Opsional |
| `journalNum` | `string` | Opsional |
| `paymentType` | `json.RawMessage` | Opsional |
| `flagAdvise` | `string` | Opsional |
| `subCompany` | `string` | Opsional |
| `billDetails` | `[]BillDetail` | Opsional |
| `freeTexts` | `[]LocalizedText` | Opsional |
| `paymentFlagReason` | `*LocalizedText` | Opsional |
| `paymentFlagStatus` | `string` | Opsional |
| `additionalInfo` | `json.RawMessage` | Opsional |

</Accordion>

<Accordion title="Field BillDetail">
BillDetail adalah satu entri dalam array "billDetails[]" pada sub-grup Virtual Account (maksimal 24 entri). Semua field tidak ditandai M/O di sumbernya sehingga semua memakai omitempty; BillDescription, BillAmount, dan Reason berupa pointer karena omitempty tidak berpengaruh pada nilai struct non-pointer. BillReferenceNo didokumentasikan Numeric pada tab Guides; sesuai aturan tipe-ambigu package ini, field Numeric yang terdokumentasi diberi tipe json.RawMessage, bukan string, karena field string biasa akan menggagalkan seluruh decode jika penerbit mengirimnya sebagai angka JSON polos, mengikuti presedan AccountTransactionLimit/APIKey yang sudah ada. BillDetail juga dipakai pada request body (CreateVARequest.BillDetails, UpdateVARequest.BillDetails), sehingga pemanggil yang mengisi BillReferenceNo harus menyertakan nilai JSON yang lengkap (mis. json.RawMessage(`"BILLREF1"`) atau json.RawMessage(`123`)), bukan string Go polos — nilai yang tidak lengkap akan menggagalkan json.Marshal untuk seluruh request.

| Field | Type | Presence |
|---|---|---|
| `billCode` | `string` | Opsional |
| `billNo` | `string` | Opsional |
| `billName` | `string` | Opsional |
| `billShortName` | `string` | Opsional |
| `billDescription` | `*LocalizedText` | Opsional |
| `billSubCompany` | `string` | Opsional |
| `billAmount` | `*snap.Money` | Opsional |
| `additionalInfo` | `json.RawMessage` | Opsional |
| `billAmountLabel` | `string` | Opsional |
| `billAmountValue` | `string` | Opsional |
| `billReferenceNo` | `json.RawMessage` | Opsional |
| `status` | `string` | Opsional |
| `reason` | `*LocalizedText` | Opsional |

</Accordion>

<Accordion title="Field LocalizedText">
LocalizedText adalah bentuk teks dwibahasa \{english, indonesia\} bersama yang dipakai di seluruh sub-grup Virtual Account (mis. inquiryReason, paymentFlagReason, billDescription, reason per-bill, entri freeTexts[]). Kedua field bertipe String, tidak ditandai M/O di sumbernya, sehingga keduanya memakai omitempty.

| Field | Type | Presence |
|---|---|---|
| `english` | `string` | Opsional |
| `indonesia` | `string` | Opsional |

</Accordion>


---

### `VAInquiryStatus`

VAInquiryStatus memanggil endpoint SNAP VA - VA Inquiry Status (Service Code 26, path .../\{version\}/transfer-va/status, HTTP POST). hb harus sudah membawa setiap field yang dibutuhkan snap.HeaderBuilder kecuali Body, yang diisi sendiri oleh VAInquiryStatus agar byte hasil marshal yang persis sama dipakai untuk signing maupun wire body.

```go
func VAInquiryStatus(ctx context.Context, t *snap.Transport, hb snap.HeaderBuilder, req VAInquiryStatusRequest) (VAInquiryStatusResponse, error)
```

**Request &mdash; `VAInquiryStatusRequest`**

VAInquiryStatusRequest adalah request body untuk API VA - VA Inquiry Status (Service Code 26). PartnerServiceID, CustomerNo, dan VirtualAccountNo (trio identitas) wajib menurut tab Guides; InquiryRequestID dan PaymentRequestID sama-sama Opsional/Kondisional (omitempty). Dokumentasi portal menyebut "jika InquiryRequestID tidak dikirim, mengembalikan array berdasarkan virtualAccountNo," namun di bagian lain response didokumentasikan sebagai satu Object tunggal — package ini mengikuti bacaan Object tunggal dan tidak menebak-nebak bentuk array tersebut.

| Field | Type | Presence |
|---|---|---|
| `partnerServiceId` | `string` | Wajib |
| `customerNo` | `json.RawMessage` | Wajib |
| `virtualAccountNo` | `string` | Wajib |
| `inquiryRequestId` | `string` | Opsional |
| `paymentRequestId` | `string` | Opsional |
| `additionalInfo` | `json.RawMessage` | Opsional |

**Response &mdash; `VAInquiryStatusResponse`**

VAInquiryStatusResponse adalah response body untuk API VA - VA Inquiry Status.

| Field | Type | Presence |
|---|---|---|
| `responseCode` | `string` | Wajib |
| `responseMessage` | `string` | Wajib |
| `virtualAccountData` | `*VAInquiryStatusData` | Opsional |

<Accordion title="Field VAInquiryStatusData">
VAInquiryStatusData adalah objek "virtualAccountData" pada VAInquiryStatusResponse — mencerminkan bentuk response VA Payment ditambah transactionDate. Ini adalah tipe yang berbeda dari VAPaymentData (bukan pemakaian ulang), mengikuti konvensi package ini "tipe berbeda untuk setiap service code walau bentuknya identik/nyaris identik"; transfer_shared_types_test.go menjaga keduanya agar tidak melenceng pada field/tag.

| Field | Type | Presence |
|---|---|---|
| `partnerServiceId` | `string` | Opsional |
| `customerNo` | `json.RawMessage` | Opsional |
| `virtualAccountNo` | `string` | Opsional |
| `trxId` | `string` | Opsional |
| `paymentRequestId` | `string` | Opsional |
| `channelCode` | `json.RawMessage` | Opsional |
| `hashedSourceAccountNo` | `string` | Opsional |
| `sourceBankCode` | `string` | Opsional |
| `paidAmount` | `*snap.Money` | Opsional |
| `cumulativePaymentAmount` | `*snap.Money` | Opsional |
| `paidBills` | `string` | Opsional |
| `totalAmount` | `*snap.Money` | Opsional |
| `trxDateTime` | `string` | Opsional |
| `referenceNo` | `string` | Opsional |
| `journalNum` | `string` | Opsional |
| `paymentType` | `json.RawMessage` | Opsional |
| `flagAdvise` | `string` | Opsional |
| `subCompany` | `string` | Opsional |
| `billDetails` | `[]BillDetail` | Opsional |
| `freeTexts` | `[]LocalizedText` | Opsional |
| `paymentFlagReason` | `*LocalizedText` | Opsional |
| `paymentFlagStatus` | `string` | Opsional |
| `additionalInfo` | `json.RawMessage` | Opsional |
| `transactionDate` | `string` | Opsional |

</Accordion>

<Accordion title="Field BillDetail">
BillDetail adalah satu entri dalam array "billDetails[]" pada sub-grup Virtual Account (maksimal 24 entri). Semua field tidak ditandai M/O di sumbernya sehingga semua memakai omitempty; BillDescription, BillAmount, dan Reason berupa pointer karena omitempty tidak berpengaruh pada nilai struct non-pointer. BillReferenceNo didokumentasikan Numeric pada tab Guides; sesuai aturan tipe-ambigu package ini, field Numeric yang terdokumentasi diberi tipe json.RawMessage, bukan string, karena field string biasa akan menggagalkan seluruh decode jika penerbit mengirimnya sebagai angka JSON polos, mengikuti presedan AccountTransactionLimit/APIKey yang sudah ada. BillDetail juga dipakai pada request body (CreateVARequest.BillDetails, UpdateVARequest.BillDetails), sehingga pemanggil yang mengisi BillReferenceNo harus menyertakan nilai JSON yang lengkap (mis. json.RawMessage(`"BILLREF1"`) atau json.RawMessage(`123`)), bukan string Go polos — nilai yang tidak lengkap akan menggagalkan json.Marshal untuk seluruh request.

| Field | Type | Presence |
|---|---|---|
| `billCode` | `string` | Opsional |
| `billNo` | `string` | Opsional |
| `billName` | `string` | Opsional |
| `billShortName` | `string` | Opsional |
| `billDescription` | `*LocalizedText` | Opsional |
| `billSubCompany` | `string` | Opsional |
| `billAmount` | `*snap.Money` | Opsional |
| `additionalInfo` | `json.RawMessage` | Opsional |
| `billAmountLabel` | `string` | Opsional |
| `billAmountValue` | `string` | Opsional |
| `billReferenceNo` | `json.RawMessage` | Opsional |
| `status` | `string` | Opsional |
| `reason` | `*LocalizedText` | Opsional |

</Accordion>

<Accordion title="Field LocalizedText">
LocalizedText adalah bentuk teks dwibahasa \{english, indonesia\} bersama yang dipakai di seluruh sub-grup Virtual Account (mis. inquiryReason, paymentFlagReason, billDescription, reason per-bill, entri freeTexts[]). Kedua field bertipe String, tidak ditandai M/O di sumbernya, sehingga keduanya memakai omitempty.

| Field | Type | Presence |
|---|---|---|
| `english` | `string` | Opsional |
| `indonesia` | `string` | Opsional |

</Accordion>


---

### `VAInquiryPaymentIntrabank`

VAInquiryPaymentIntrabank memanggil endpoint SNAP VA - Inquiry Payment Intrabank (Service Code 32, path .../\{version\}/transfer-va/inquiry-intrabank, HTTP POST). hb harus sudah membawa setiap field yang dibutuhkan snap.HeaderBuilder kecuali Body, yang diisi sendiri oleh VAInquiryPaymentIntrabank agar byte hasil marshal yang persis sama dipakai untuk signing maupun wire body.

```go
func VAInquiryPaymentIntrabank(ctx context.Context, t *snap.Transport, hb snap.HeaderBuilder, req VAInquiryPaymentIntrabankRequest) (VAInquiryPaymentIntrabankResponse, error)
```

**Request &mdash; `VAInquiryPaymentIntrabankRequest`**

VAInquiryPaymentIntrabankRequest adalah request body untuk API VA - Inquiry Payment Intrabank (Service Code 32). PartnerServiceID, CustomerNo, dan VirtualAccountNo (trio identitas) wajib menurut tab Guides. CustomerNo bertipe json.RawMessage: contoh endpoint ini sendiri menampilkannya sebagai angka JSON polos. Hanya field yang secara eksplisit disebutkan untuk endpoint ini yang dimodelkan; billDetails/freeTexts/totalAmount/feeAmount tidak disebutkan ulang untuk endpoint 32 sebagaimana untuk endpoint 24, sehingga tidak ditambahkan di sini.

| Field | Type | Presence |
|---|---|---|
| `partnerServiceId` | `string` | Wajib |
| `customerNo` | `json.RawMessage` | Wajib |
| `virtualAccountNo` | `string` | Wajib |
| `partnerReferenceNo` | `string` | Opsional |
| `sourceAccountNo` | `string` | Opsional |
| `sourceAccountType` | `string` | Opsional |
| `additionalInfo` | `json.RawMessage` | Opsional |

**Response &mdash; `VAInquiryPaymentIntrabankResponse`**

VAInquiryPaymentIntrabankResponse adalah response body untuk API VA - Inquiry Payment Intrabank.

| Field | Type | Presence |
|---|---|---|
| `responseCode` | `string` | Wajib |
| `responseMessage` | `string` | Wajib |
| `virtualAccountdata` | `*VAInquiryPaymentIntrabankData` | Opsional |

<Accordion title="Field VAInquiryPaymentIntrabankData">
VAInquiryPaymentIntrabankData adalah objek "virtualAccountdata" (huruf d kecil — bukan typo tabel, berbeda dari tipe-tipe VA lain yang memakai virtualAccountData berhuruf D besar) pada VAInquiryPaymentIntrabankResponse.

| Field | Type | Presence |
|---|---|---|
| `partnerServiceId` | `string` | Opsional |
| `customerNo` | `json.RawMessage` | Opsional |
| `virtualAccountNo` | `string` | Opsional |
| `partnerReferenceNo` | `string` | Opsional |
| `sourceAccountNo` | `string` | Opsional |
| `sourceAccountType` | `string` | Opsional |
| `productName` | `string` | Opsional |
| `billAmountLabel` | `string` | Opsional |
| `billAmountValue` | `string` | Opsional |
| `additionalInfo` | `json.RawMessage` | Opsional |

</Accordion>


---

### `VAPaymentIntrabank`

VAPaymentIntrabank memanggil endpoint SNAP VA - Payment Intrabank endpoint (Service Code 33, path .../\{version\}/transfer-va/payment-intrabank, HTTP POST — no method override). hb harus sudah membawa setiap field yang dibutuhkan snap.HeaderBuilder kecuali Body, yang diisi sendiri oleh VAPaymentIntrabank agar byte hasil marshal yang persis sama dipakai untuk signing maupun wire body.

Operasi ini tidak idempotent dan package ini tidak melakukan retry. Pemanggil yang mengulang panggilan yang gagal atau timeout harus memakai X-EXTERNAL-ID yang sama, karena deteksi duplikat di sisi server berpatokan pada header itu.

```go
func VAPaymentIntrabank(ctx context.Context, t *snap.Transport, hb snap.HeaderBuilder, req VAPaymentIntrabankRequest) (VAPaymentIntrabankResponse, error)
```

**Request &mdash; `VAPaymentIntrabankRequest`**

VAPaymentIntrabankRequest adalah request body untuk API VA - Payment Intrabank (Service Code 33). PartnerServiceID, CustomerNo, VirtualAccountNo, PartnerReferenceNo, dan PaidAmount wajib menurut tab Guides. CustomerNo bertipe json.RawMessage (contoh menampilkan angka polos). ReferenceNo juga json.RawMessage: contoh request endpoint ini menampilkannya sebagai angka polos, meski pada response-nya ditampilkan berupa string berkutip. PaymentStatus adalah string status bebas, secara eksplisit berbeda dari enum 2-digit transactionStatus milik package ini.

| Field | Type | Presence |
|---|---|---|
| `partnerServiceId` | `string` | Wajib |
| `customerNo` | `json.RawMessage` | Wajib |
| `virtualAccountNo` | `string` | Wajib |
| `sourceAccountNo` | `string` | Opsional |
| `sourceAccountType` | `string` | Opsional |
| `inquiryRequestId` | `string` | Opsional |
| `partnerReferenceNo` | `string` | Wajib |
| `paidAmount` | `snap.Money` | Wajib |
| `cumulativePaymentAmount` | `*snap.Money` | Opsional |
| `paidBills` | `string` | Opsional |
| `paymentStatus` | `string` | Opsional |
| `referenceNo` | `json.RawMessage` | Opsional |
| `additionalInfo` | `json.RawMessage` | Opsional |

**Response &mdash; `VAPaymentIntrabankResponse`**

VAPaymentIntrabankResponse adalah response body untuk API VA - Payment Intrabank.

| Field | Type | Presence |
|---|---|---|
| `responseCode` | `string` | Wajib |
| `responseMessage` | `string` | Wajib |
| `virtualAccountdata` | `*VAPaymentIntrabankData` | Opsional |

<Accordion title="Field VAPaymentIntrabankData">
VAPaymentIntrabankData adalah objek "virtualAccountdata" (huruf d kecil) pada VAPaymentIntrabankResponse — mencerminkan field-field VAPaymentIntrabankRequest, mengikuti konvensi request-tercermin-ke-response yang sama seperti pada VAPaymentData.

| Field | Type | Presence |
|---|---|---|
| `partnerServiceId` | `string` | Opsional |
| `customerNo` | `json.RawMessage` | Opsional |
| `virtualAccountNo` | `string` | Opsional |
| `sourceAccountNo` | `string` | Opsional |
| `sourceAccountType` | `string` | Opsional |
| `inquiryRequestId` | `string` | Opsional |
| `partnerReferenceNo` | `string` | Opsional |
| `paidAmount` | `*snap.Money` | Opsional |
| `cumulativePaymentAmount` | `*snap.Money` | Opsional |
| `paidBills` | `string` | Opsional |
| `paymentStatus` | `string` | Opsional |
| `referenceNo` | `json.RawMessage` | Opsional |
| `additionalInfo` | `json.RawMessage` | Opsional |

</Accordion>


---

### `VANotifyPaymentIntrabank`

VANotifyPaymentIntrabank memanggil endpoint SNAP VA - Notify Payment Intrabank endpoint (Service Code 34, path .../\{version\}/transfer-va/notify-payment-intrabank, HTTP POST — no method override). hb harus sudah membawa setiap field yang dibutuhkan snap.HeaderBuilder kecuali Body, yang diisi sendiri oleh VANotifyPaymentIntrabank agar byte hasil marshal yang persis sama dipakai untuk signing maupun wire body.

Operasi ini tidak idempotent dan package ini tidak melakukan retry. Pemanggil yang mengulang panggilan yang gagal atau timeout harus memakai X-EXTERNAL-ID yang sama, karena deteksi duplikat di sisi server berpatokan pada header itu.

```go
func VANotifyPaymentIntrabank(ctx context.Context, t *snap.Transport, hb snap.HeaderBuilder, req VANotifyPaymentIntrabankRequest) (VANotifyPaymentIntrabankResponse, error)
```

**Request &mdash; `VANotifyPaymentIntrabankRequest`**

VANotifyPaymentIntrabankRequest adalah request body untuk API VA - Notify Payment Intrabank (Service Code 34). Ini adalah "callback yang dikirim keluar oleh PJP" — PJP yang memulai panggilan ini, berbeda dari endpoint notifikasi RTGS/SKNBI/Interbank-Bulk yang hanya diterima PJP. Karena itu fungsi ini adalah fungsi pemanggil biasa, bukan tipe inbound berupa struct saja. PartnerServiceID terdokumentasi "StringNumber" (typo pada tabel sumber) tetapi ditampilkan berkutip pada contoh endpoint ini, sehingga tetap bertipe string. CustomerNo bertipe json.RawMessage (contoh menampilkan angka polos).

| Field | Type | Presence |
|---|---|---|
| `partnerServiceId` | `string` | Wajib |
| `customerNo` | `json.RawMessage` | Wajib |
| `virtualAccountNo` | `string` | Wajib |
| `inquiryRequestId` | `string` | Opsional |
| `paymentRequestId` | `string` | Opsional |
| `partnerReferenceNo` | `string` | Opsional |
| `trxDateTime` | `string` | Opsional |
| `paymentStatus` | `string` | Opsional |
| `paymentFlagReason` | `*LocalizedText` | Opsional |
| `additionalInfo` | `json.RawMessage` | Opsional |

<Accordion title="Field LocalizedText">
LocalizedText adalah bentuk teks dwibahasa \{english, indonesia\} bersama yang dipakai di seluruh sub-grup Virtual Account (mis. inquiryReason, paymentFlagReason, billDescription, reason per-bill, entri freeTexts[]). Kedua field bertipe String, tidak ditandai M/O di sumbernya, sehingga keduanya memakai omitempty.

| Field | Type | Presence |
|---|---|---|
| `english` | `string` | Opsional |
| `indonesia` | `string` | Opsional |

</Accordion>

**Response &mdash; `VANotifyPaymentIntrabankResponse`**

VANotifyPaymentIntrabankResponse adalah response body untuk API VA - Notify Payment Intrabank.

| Field | Type | Presence |
|---|---|---|
| `responseCode` | `string` | Wajib |
| `responseMessage` | `string` | Wajib |
| `virtualAccountdata` | `*VANotifyPaymentIntrabankData` | Opsional |

<Accordion title="Field VANotifyPaymentIntrabankData">
VANotifyPaymentIntrabankData adalah objek "virtualAccountdata" (huruf d kecil) pada VANotifyPaymentIntrabankResponse — mencerminkan sebagian besar field VANotifyPaymentIntrabankRequest ditambah envelope.

| Field | Type | Presence |
|---|---|---|
| `partnerServiceId` | `string` | Opsional |
| `customerNo` | `json.RawMessage` | Opsional |
| `virtualAccountNo` | `string` | Opsional |
| `inquiryRequestId` | `string` | Opsional |
| `paymentRequestId` | `string` | Opsional |
| `partnerReferenceNo` | `string` | Opsional |
| `trxDateTime` | `string` | Opsional |
| `paymentStatus` | `string` | Opsional |
| `paymentFlagReason` | `*LocalizedText` | Opsional |
| `additionalInfo` | `json.RawMessage` | Opsional |

</Accordion>

<Accordion title="Field LocalizedText">
LocalizedText adalah bentuk teks dwibahasa \{english, indonesia\} bersama yang dipakai di seluruh sub-grup Virtual Account (mis. inquiryReason, paymentFlagReason, billDescription, reason per-bill, entri freeTexts[]). Kedua field bertipe String, tidak ditandai M/O di sumbernya, sehingga keduanya memakai omitempty.

| Field | Type | Presence |
|---|---|---|
| `english` | `string` | Opsional |
| `indonesia` | `string` | Opsional |

</Accordion>


---

### `VAGetReport`

VAGetReport memanggil endpoint SNAP VA - Get Report (Service Code 35, path .../\{version\}/transfer-va/report). hb harus sudah membawa setiap field yang dibutuhkan snap.HeaderBuilder kecuali Method dan Body: VAGetReport mengatur Method menjadi POST sendiri — ini satu-satunya endpoint VA di mana spesifikasi sumbernya kontradiktif dengan dirinya sendiri (GET menurut tab Guides, POST-dengan-body menurut contoh kode), dan package ini memilih POST, sehingga method tidak bisa dikonfigurasi pemanggil, sama seperti endpoint PUT/DELETE lain yang method-nya sudah tetap — dan Body, agar byte hasil marshal yang persis sama dipakai untuk signing maupun wire body.

Report yang mencakup rentang tanggal lebar bisa mengembalikan jumlah entri array yang tidak terbatas; lapisan transport bersama membatasi setiap response body hingga 10 MiB (lihat transport.go), sehingga pemanggil yang mengambil report besar sebaiknya melakukan paging dengan rentang tanggal/waktu yang lebih sempit, bukan satu panggilan tak terbatas.

```go
func VAGetReport(ctx context.Context, t *snap.Transport, hb snap.HeaderBuilder, req VAGetReportRequest) (VAGetReportResponse, error)
```

**Request &mdash; `VAGetReportRequest`**

VAGetReportRequest adalah request body untuk API VA - Get Report (Service Code 35). PartnerServiceID adalah satu-satunya field wajib menurut tab Guides; tipenya berubah menjadi Number khusus untuk endpoint ini (String di tempat lain pada package), sehingga diberi tipe json.RawMessage. Endpoint ini terdokumentasi baik sebagai GET (tab Guides) maupun POST-dengan-body (contoh kode); package ini memodelkannya sebagai POST dengan JSON body, sama seperti endpoint lain dalam grup ini.

| Field | Type | Presence |
|---|---|---|
| `partnerServiceId` | `json.RawMessage` | Wajib |
| `startDate` | `string` | Opsional |
| `startTime` | `string` | Opsional |
| `endDate` | `string` | Opsional |
| `endTime` | `string` | Opsional |
| `additionalInfo` | `json.RawMessage` | Opsional |

**Response &mdash; `VAGetReportResponse`**

VAGetReportResponse adalah response body untuk API VA - Get Report. Ini satu-satunya response VA pada package ini yang field data-nya berupa array, bukan objek tunggal.

| Field | Type | Presence |
|---|---|---|
| `responseCode` | `string` | Wajib |
| `responseMessage` | `string` | Wajib |
| `virtualAccountdata` | `[]GetReportData` | Opsional |

<Accordion title="Field GetReportData">
GetReportData adalah satu entri dalam array "virtualAccountdata" (huruf d kecil) pada VAGetReportResponse — field-nya identik dengan VAInquiryStatusData, karena "setiap item berbentuk seperti objek response Payment/Inquiry-Status." Ini adalah tipe yang berbeda (bukan pemakaian ulang), mengikuti konvensi package ini "tipe berbeda untuk setiap service code walau bentuknya identik"; transfer_shared_types_test.go menjaga keduanya lewat pengecekan kesetaraan penuh agar tidak melenceng pada field/tag.

| Field | Type | Presence |
|---|---|---|
| `partnerServiceId` | `string` | Opsional |
| `customerNo` | `json.RawMessage` | Opsional |
| `virtualAccountNo` | `string` | Opsional |
| `trxId` | `string` | Opsional |
| `paymentRequestId` | `string` | Opsional |
| `channelCode` | `json.RawMessage` | Opsional |
| `hashedSourceAccountNo` | `string` | Opsional |
| `sourceBankCode` | `string` | Opsional |
| `paidAmount` | `*snap.Money` | Opsional |
| `cumulativePaymentAmount` | `*snap.Money` | Opsional |
| `paidBills` | `string` | Opsional |
| `totalAmount` | `*snap.Money` | Opsional |
| `trxDateTime` | `string` | Opsional |
| `referenceNo` | `string` | Opsional |
| `journalNum` | `string` | Opsional |
| `paymentType` | `json.RawMessage` | Opsional |
| `flagAdvise` | `string` | Opsional |
| `subCompany` | `string` | Opsional |
| `billDetails` | `[]BillDetail` | Opsional |
| `freeTexts` | `[]LocalizedText` | Opsional |
| `paymentFlagReason` | `*LocalizedText` | Opsional |
| `paymentFlagStatus` | `string` | Opsional |
| `additionalInfo` | `json.RawMessage` | Opsional |
| `transactionDate` | `string` | Opsional |

</Accordion>

<Accordion title="Field BillDetail">
BillDetail adalah satu entri dalam array "billDetails[]" pada sub-grup Virtual Account (maksimal 24 entri). Semua field tidak ditandai M/O di sumbernya sehingga semua memakai omitempty; BillDescription, BillAmount, dan Reason berupa pointer karena omitempty tidak berpengaruh pada nilai struct non-pointer. BillReferenceNo didokumentasikan Numeric pada tab Guides; sesuai aturan tipe-ambigu package ini, field Numeric yang terdokumentasi diberi tipe json.RawMessage, bukan string, karena field string biasa akan menggagalkan seluruh decode jika penerbit mengirimnya sebagai angka JSON polos, mengikuti presedan AccountTransactionLimit/APIKey yang sudah ada. BillDetail juga dipakai pada request body (CreateVARequest.BillDetails, UpdateVARequest.BillDetails), sehingga pemanggil yang mengisi BillReferenceNo harus menyertakan nilai JSON yang lengkap (mis. json.RawMessage(`"BILLREF1"`) atau json.RawMessage(`123`)), bukan string Go polos — nilai yang tidak lengkap akan menggagalkan json.Marshal untuk seluruh request.

| Field | Type | Presence |
|---|---|---|
| `billCode` | `string` | Opsional |
| `billNo` | `string` | Opsional |
| `billName` | `string` | Opsional |
| `billShortName` | `string` | Opsional |
| `billDescription` | `*LocalizedText` | Opsional |
| `billSubCompany` | `string` | Opsional |
| `billAmount` | `*snap.Money` | Opsional |
| `additionalInfo` | `json.RawMessage` | Opsional |
| `billAmountLabel` | `string` | Opsional |
| `billAmountValue` | `string` | Opsional |
| `billReferenceNo` | `json.RawMessage` | Opsional |
| `status` | `string` | Opsional |
| `reason` | `*LocalizedText` | Opsional |

</Accordion>

<Accordion title="Field LocalizedText">
LocalizedText adalah bentuk teks dwibahasa \{english, indonesia\} bersama yang dipakai di seluruh sub-grup Virtual Account (mis. inquiryReason, paymentFlagReason, billDescription, reason per-bill, entri freeTexts[]). Kedua field bertipe String, tidak ditandai M/O di sumbernya, sehingga keduanya memakai omitempty.

| Field | Type | Presence |
|---|---|---|
| `english` | `string` | Opsional |
| `indonesia` | `string` | Opsional |

</Accordion>

