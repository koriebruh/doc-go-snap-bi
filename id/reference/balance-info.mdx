---
title: "Info Saldo"
description: "Informasi Saldo — Service Code 11 — 1 endpoint"
---

## Info Saldo

Kategori terkecil di portal: satu endpoint, satu file, satu package, di `balanceinfo`.

<CodeGroup>
```go example.go
resp, err := balanceinfo.BalanceInquiry(ctx, transport, hb, balanceinfo.BalanceInquiryRequest{
	PartnerReferenceNo: "2020102900000000000001",
	BankCardToken: "...",
	AccountNo: "1234567890",
})
if err != nil {
	// errors.Is(err, snap.ErrBadRequest), snap.ErrUnauthorized, etc.
}
```
</CodeGroup>

### `BalanceInquiry`

BalanceInquiry memanggil endpoint SNAP Balance Inquiry (Service Code 11, path .../\{version\}/balance-inquiry). hb harus sudah membawa setiap field yang dibutuhkan snap.HeaderBuilder kecuali Body, yang diisi sendiri oleh BalanceInquiry agar byte hasil marshal yang sama dipakai untuk signing maupun wire body.

```go
func BalanceInquiry(ctx context.Context, t *snap.Transport, hb snap.HeaderBuilder, req BalanceInquiryRequest) (BalanceInquiryResponse, error)
```

**Request &mdash; `BalanceInquiryRequest`**

BalanceInquiryRequest adalah request body untuk API Balance Inquiry (Service Code 11). Menurut standar, tepat salah satu dari BankCardToken atau AccountNo harus diisi (kecuali customer token B2B2C sudah menyediakan konteks akun) — tipe ini tidak menegakkan aturan itu sendiri, server yang memvalidasi dan menolak inquiry yang mengosongkan keduanya.

| Field | Type | Presence |
|---|---|---|
| `partnerReferenceNo` | `string` | Opsional |
| `bankCardToken` | `string` | Opsional |
| `accountNo` | `string` | Opsional |
| `balanceTypes` | `[]string` | Opsional |
| `additionalInfo` | `json.RawMessage` | Opsional |

**Response &mdash; `BalanceInquiryResponse`**

BalanceInquiryResponse adalah response body untuk API Balance Inquiry.

| Field | Type | Presence |
|---|---|---|
| `responseCode` | `string` | Wajib |
| `responseMessage` | `string` | Wajib |
| `referenceNo` | `string` | Opsional |
| `partnerReferenceNo` | `string` | Opsional |
| `accountNo` | `string` | Opsional |
| `name` | `string` | Opsional |
| `accountInfos` | `[]AccountInfo` | Opsional |
| `additionalInfo` | `json.RawMessage` | Opsional |

<Accordion title="Field AccountInfo">
AccountInfo adalah satu entri dalam array accountInfos milik BalanceInquiryResponse.

| Field | Type | Presence |
|---|---|---|
| `balanceType` | `string` | Opsional |
| `amount` | `*snap.Money` | Opsional |
| `floatAmount` | `*snap.Money` | Opsional |
| `holdAmount` | `*snap.Money` | Opsional |
| `availableBalance` | `*snap.Money` | Opsional |
| `ledgerBalance` | `*snap.Money` | Opsional |
| `currentMultilateralLimit` | `*snap.Money` | Opsional |
| `registrationStatusCode` | `string` | Opsional |
| `status` | `string` | Opsional |
| `additionalInfo` | `json.RawMessage` | Opsional |

</Accordion>
